(jbuild_version 1)

(library
 ((name changeset_lib)
  (public_name changeset_lib)
  (libraries (base str))
))

; we need the dev version of jbuilder or dune to build the
; documentation because of index.mld

(documentation
 ((package changeset_lib)
  (mld_files (index)))
)
